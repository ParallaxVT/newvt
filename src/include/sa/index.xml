<krpano>

  <action name="activatepano_scenevariation">
    loadscene(%1,null,KEEPVIEW|MERGE,BLEND(1));
    move_camera(get(xml.scene));
  </action>

  <action name="activatepano">
    loadscene(%1,null,MERGE,BLEND(1));
    move_camera(get(xml.scene));
  </action>

</krpano>